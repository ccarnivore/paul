<purpose>Create a bounded executable plan while retaining project rules and the PLAN → APPLY → UNIFY loop.</purpose>

1. Read `references/runtime.md` once if its contract is not already in context. Run `context` (add --plan for a supplied existing plan, --phase for the current phase). Use complete STATE to locate the active loop. Reconcile an applied loop before new work. Resume an unfinished plan instead of creating a duplicate.
2. Read the relevant ROADMAP scope, PROJECT sections needed for this task, `.paul/config.md` and SPECIAL-FLOWS if present. Read relevant CONTEXT/RESEARCH/DISCOVERY from context's inventory. Inspect actual source and direct dependencies. Search history for applicable decisions; open only relevant results. Unknown headings are not automatically irrelevant. Codebase maps and historical documents are optional targeted reads, not an automatic bundle.
3. Choose proportional detail: quickfix for a localized low-risk correction, standard for ordinary work, complex for substantial cross-component/risky changes. All tracks retain acceptance criteria, skills, boundaries, verification and decisions. Ask only for missing information that changes the plan; use existing authorization and preferences. Read discovery/research workflows only if material uncertainty needs them. For TDD or checkpoints, read their relevant reference on demand.
4. Use `templates/PLAN.md`. Define observable AC-N criteria, executable tasks with files/action/verify/done, required skills, dependencies, boundaries and verification strategy. State the roadmap goal and explicit remaining scope. Typical plan target: 1–3 tasks, 3–6 KB; split genuinely independent work when needed. This is a soft size target, never a reason to omit requirements. Link evidence and source instead of copying tutorials or large logs.
5. Review feasibility, task-to-AC coverage, dependency order, ownership of files and verification. No placeholder requirements or unjustified extra scope. Save `.paul/phases/{NN}-{name}/{NN}-{PP}-PLAN.md`, then `plan-ready --plan ID`. It updates state, manifest and ledger together; revisions of the same still-planned plan are supported. Never use it to reset an applied loop.
6. If the configured enterprise audit is enabled, follow audit-plan.md before APPLY. Show the goal, task/AC summary, material decisions and exact plan path. Request approval only if not already authorized. Do not execute an unapproved plan. After approval, route to /paul:apply.

## Extensions

<!-- Extension hooks may be appended here. -->
