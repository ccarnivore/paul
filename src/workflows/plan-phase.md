<purpose>Create a bounded executable plan while retaining project rules and the PLAN → APPLY → UNIFY loop.</purpose>

1. Read `references/runtime.md` once if needed. Run `context` (add --plan for an existing plan, --phase for the current phase). Use complete STATE to locate the loop. Reconcile applied work before new work; resume an unfinished plan instead of creating a duplicate. Reuse unchanged context already read in this session.
2. Read relevant ROADMAP scope, PROJECT sections, config and SPECIAL-FLOWS. Load relevant CONTEXT/RESEARCH/DISCOVERY from the inventory. Search `history --query "distinct keywords" --limit 5`; terms need not be adjacent. For a predecessor/dependency, run `handoff --plan ID` before opening its full SUMMARY/RESULT. Follow pagination and read applicable decisions, issues, deviations and evidence with `result-section` or bounded `section`. Unknown headings and unreturned hits are not proof of irrelevance. See runtime-reading.md only if retrieval details are needed.
3. Resolve material uncertainty by inspecting actual source and direct dependencies. Use the project's required discovery tools first. Batch independent symbol/path searches, then read related relevant ranges together. Keep an in-session list of answered questions and source locations; stop searching when requirements, data flow, permissions, failure cases and verification are supported. Trace further when a concrete unresolved dependency requires it. Keep large search/test output on disk and return matches, counts and relevant failure excerpts; do not pipe required context through head/tail.
4. Choose quickfix, standard or complex detail proportional to risk; retain ACs, skills, boundaries, verification and decisions on every track. Ask only for missing information that changes the plan. Before writing, settle scope and required decisions and map 1–3 tasks to observable ACs. Use templates/PLAN.md: typical 3–6 KB, with source paths and short reasons in context. State each requirement once; tasks reference AC IDs and verification lists checks. Keep tutorials, repeated rationales and code transcripts out of the plan. Extra detail needed for safe execution takes priority over size; split only genuinely independent work.
5. Save `.paul/phases/{NN}-{name}/{NN}-{PP}-PLAN.md` once the content is coherent. Run `plan-check --plan ID` for structure/size without rereading it. Review feasibility, dependencies, file ownership and coverage. If configured/requested, follow audit-plan.md. Apply corrections with focused Edit operations; do not write a second complete copy just to reduce size, and do not echo the saved plan into the conversation. A size warning is advisory, never permission to remove requirements.
6. Run `plan-ready --plan ID` for the final revision (skip a duplicate registration already done by audit). It updates STATE, manifest and ledger together; never reset an applied loop. Present goal, task/AC summary, material decisions and plan path. Request approval only if not already authorized for this revision. Do not execute an unapproved plan; after approval route to /paul:apply.

## Extensions

<!-- Extension hooks may be appended here. -->
