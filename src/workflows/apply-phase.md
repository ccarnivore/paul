<purpose>Execute the approved plan with durable evidence and preserved project safeguards.</purpose>

1. Read references/runtime.md if not already loaded. Run `context --plan ID`; read the selected PLAN and relevant project/source sections. Respect full STATE, config and SPECIAL-FLOWS. Confirm dependency outputs exist and match the plan; pending dependencies block execution. SUMMARY existence alone neither authorizes reexecution nor proves UNIFY complete. Inspect receipts/state on ambiguous legacy sessions.
2. Confirm user authorization for this exact plan revision. Record it with `approve --plan ID --signal "actual approval"`. Reuse an existing valid approval; ask only when absent or materially changed scope needs it. Invoke every required Claude Code skill before its work, using Skill or its documented slash-command interface; record invocation. Quickfix does not bypass skills or boundaries.
3. Execute tasks in order. Resume from the plan's APPLY-LOG after interruption, checking actual files and evidence before treating a task as done. For each task: perform action, run its verification, assess done criteria, and append a concise status/evidence/decision entry to `{id}-APPLY-LOG.md` immediately. Keep complete command logs on disk and only relevant results in conversation. Research tasks still need verifiable deliverables. For plan_type tdd, load the TDD reference and follow red → green → refactor with measured results. If required context is missing, record NEEDS_CONTEXT in APPLY-LOG, identify exactly what is missing and pause; this is not a completed task.
4. For checkpoint tasks, read references/checkpoints.md on demand: decision waits for selection, human-verify waits for approval, human-action waits for completion. Never mark a checkpoint complete from silence. Fix routine defects within approved scope; record deviations. Stop for architecture/scope changes or unmet boundaries; do not silently widen scope. Honor the project's git policy; no automatic commit, push or merge without existing authorization.
5. Verify all ACs using actual outputs. Follow project requirements for fresh tests and final build checks even if earlier evidence exists. Record failed criteria, deferred issues, concerns, decisions and skill use. Do not report estimated test counts or suppress failures.
6. Read references/runtime-result.md; write compact EVIDENCE.json covering every AC/task and changed file, then `apply-complete --plan ID --input PATH`. This records RESULT and APPLY state; UNIFY owns SUMMARY. Report outcome, verification and unresolved issues, then route to /paul:unify. An interrupted run remains resumable through APPLY-LOG; do not mark it complete prematurely.

## Extensions

<!-- Extension hooks may be appended here. -->
